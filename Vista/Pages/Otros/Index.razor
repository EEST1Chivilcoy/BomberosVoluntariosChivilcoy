@using Vista.Helpers;
@using Microsoft.AspNetCore.Authorization
@inject NavigationManager navigationManager
@page "/fire-force/"


<style>
    /* Dashboard Container */
    .dashboard-container {
        padding: 24px;
        background-color: #f0f2f5;
        min-height: 100vh;
    }

    /* Banner Section */
    .banner-section {
        margin-bottom: 24px;
    }

    .banner-card {
        position: relative;
        overflow: hidden;
        border-radius: 16px !important;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1) !important;
        height: 300px;
    }

    .banner-image {
        position: absolute; /* Posiciona la imagen para que ocupe todo el espacio */
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: 1; /* La imagen permanece detrás del overlay */
    }

    .banner-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.7));
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
        z-index: 2; /* El overlay se coloca encima de la imagen */
    }

        .banner-overlay h1 {
            color: white;
            font-size: 2.5rem;
            text-align: center;
            font-weight: 700;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
            letter-spacing: -1px;
        }

    .banner-subtitle {
        color: white;
        font-size: 2.0rem;
        text-align: center;
        font-weight: 700;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
        letter-spacing: -1px;
    }

    /* Dashboard Grid */
    .dashboard-grid {
        margin-top: 24px;
    }

    /* Cards */
    .dashboard-card {
        height: 230px;
        border-radius: 16px !important;
        transition: all 0.4s ease;
        cursor: pointer;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: -25px !important;
        box-shadow: 0 8px 20px rgba(0,0,0,0.08) !important;
        position: relative;
        overflow: hidden;
    }

        .dashboard-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(255,255,255,0.1), rgba(255,255,255,0.05));
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .dashboard-card:hover {
            transform: translateY(-1px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.12);
        }

            .dashboard-card:hover::before {
                opacity: 1;
            }

    .card-icon {
        font-size: 36px;
        margin-bottom: 16px;
        opacity: 0.9;
        transition: transform 0.3s ease;
    }

    .dashboard-card:hover .card-icon {
        transform: scale(1.1);
    }

    .dashboard-card h2 {
        margin: 12px 0;
        font-size: 1.5rem;
        font-weight: 600;
        letter-spacing: -0.5px;
        color: white !important;
    }

    .dashboard-card p {
        margin: 0;
        font-size: 0.9rem;
        color: white !important;
    }

    /* Specific Cards Color Schemes */
    .brigadas-card {
        background: #2c3e50 !important; /* Azul profundo serio */
        color: white !important;
    }

    .legajo-card {
        background: #1e3a8a !important; /* Azul oscuro serio */
        color: white !important;
    }

    .licencias-card {
        background: #14532d !important; /* Verde oscuro */
        color: white !important;
    }

    .salidas-card {
        background: #7f1d1d !important; /* Rojo oscuro serio */
        color: white !important;
    }

    .personal-card {
        background: #4b5563 !important; /* Gris oscuro */
        color: white !important;
    }

    .cobradores-card {
        background: #065f46 !important; /* Verde esmeralda oscuro */
        color: white !important;
    }

    .unidades-card {
        background: #92400e !important; /* Naranja marrón */
        color: white !important;
    }

    .firmas-card {
        background: #0f766e !important; /* Verde azulado oscuro */
        color: white !important;
    }

    .reportes-card {
        background: #1e293b !important; /* Azul grisáceo oscuro */
        color: white !important;
    }

    .departamentos-card {
        background: #3730a3 !important; /* Azul violeta oscuro */
        color: white !important;
    }

    .etica-card {
        background: #6b7280 !important; /* Gris oscuro */
        color: white !important;
    }

    .fuerzas-intervinientes-card {
        background: #5d4037 !important; /* Marrón oscuro serio */
        color: white !important;
    }

    .department-buttons, .ethics-buttons, .report-buttons {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 15px;
        width: 100%;
    }

        .department-buttons .ant-btn,
        .ethics-buttons .ant-btn,
        .report-buttons .ant-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            border-radius: 12px;
            background-color: rgba(255,255,255,0.2);
            color: white;
            border: none;
            transition: all 0.3s ease;
        }

            .department-buttons .ant-btn:hover,
            .ethics-buttons .ant-btn:hover,
            .report-buttons .ant-btn:hover {
                background-color: rgba(255,255,255,0.3);
                transform: scale(1.1);
            }

            .department-buttons .ant-btn i,
            .ethics-buttons .ant-btn i,
            .report-buttons .ant-btn i {
                font-size: 20px;
            }

    /* Responsive Adjustments */
    @@media (max-width: 768px) {
        .banner-overlay h1 {
            font-size: 1.8rem;
        }

        .banner-subtitle {
            font-size: 1.2rem;
            font: bolder;
        }

        .dashboard-card {
            height: 200px;
        }

        .card-icon {
            font-size: 28px;
        }

        .dashboard-card h2 {
            font-size: 1.2rem;
        }
    }
</style>

<div class="dashboard-container">
    @*Banner Section*@
    <Row Class="banner-section">
        <AntDesign.Col Span="24">
            <Card Bordered="false" Class="banner-card">
                <img src="Imagenes/banner_index.png" class="banner-image" alt="Banner Bomberos" />
                <div class="banner-overlay">
                    <div>
                        <h1>SISTEMA DE GESTIÓN DE BOMBEROS</h1>
                        <div class="banner-subtitle">Bomberos Voluntarios de Chivilcoy</div>
                    </div>
                </div>
            </Card>
        </AntDesign.Col>
    </Row>

    @*Main Grid Section*@
    <Row Gutter="24" Class="dashboard-grid">

        @*Legajo Section*@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Bomberos/Legajo"); })" style="cursor: pointer;">
                <Card Class="dashboard-card legajo-card">
                    <i class="fa-solid fa-user card-icon"></i>
                    <h2>LEGAJO</h2>
                    <p>Visite su legajo personal</p>
                </Card>
            </div>
        </AntDesign.Col>

        @*Salidas Section*@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Salidas"); })" style="cursor: pointer;">
                <Card Class="dashboard-card salidas-card">
                    <Icon class="card-icon" Type="alert" Theme="IconThemeType.Fill" />
                    <h2>SALIDAS</h2>
                    <p>Gestión de salidas</p>
                </Card>
            </div>
        </AntDesign.Col>

        @* Personal/Bomberos Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/personal/bomberos"); })" style="cursor: pointer;">
                <Card Class="dashboard-card personal-card">
                    <i class="fa-solid fa-people-group card-icon"></i>
                    <h2>BOMBEROS</h2>
                    <p>Ver el personal de bomberos</p>
                </Card>
            </div>
        </AntDesign.Col>

        @* Comisión Directiva Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/personal/comisiondirectiva"); })" style="cursor: pointer;">
                <Card Class="dashboard-card personal-card">
                    <i class="fa-solid fa-user-tie card-icon"></i>
                    <h2>COMISIÓN DIRECTIVA</h2>
                    <p>Ver la comisión directiva</p>
                </Card>
            </div>
        </AntDesign.Col>

        @*Licencias Section*@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Licencias"); })" style="cursor: pointer;">
                <Card Class="dashboard-card licencias-card">
                    <i class="fa-solid fa-file-signature card-icon"></i>
                    <h2>LICENCIAS</h2>
                    <p>Gestión de licencias</p>
                </Card>
            </div>
        </AntDesign.Col>

        @* Unidades Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/unidades"); })" style="cursor: pointer;">
                <Card Class="dashboard-card unidades-card">
                    <i class="fa-solid fa-truck card-icon"></i>
                    <h2>UNIDADES</h2>
                    <p>Ver las unidades</p>
                </Card>
            </div>
        </AntDesign.Col>

        @* Firmas Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/incidentes-vehiculos"); })" style="cursor: pointer;">
                <Card Class="dashboard-card firmas-card">
                    <i class="fa-solid fa-scroll card-icon"></i>
                    <h2>FIRMAS</h2>
                    <p>Ver firmas</p>
                </Card>
            </div>
        </AntDesign.Col>

        @* Ética Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <Card Class="dashboard-card etica-card">
                <h2>ÉTICA</h2>
                <div class="ethics-buttons">
                    <span title="Sancionados">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/bomberos/sancionados"); })">
                            <i class="fa-solid fa-user-xmark"></i>
                        </Button>
                    </span>
                    <span title="Ascensos">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/bomberos/ascenso"); })">
                            <i class="fa-solid fa-user-plus"></i>
                        </Button>
                    </span>
                </div>
            </Card>
        </AntDesign.Col>

        @* Reportes Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <Card Class="dashboard-card reportes-card">
                <h2>REPORTES</h2>
                <div class="report-buttons">
                    <span title="Historial Bomberos">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Reportes/HistorialBomberos"); })">
                            <i class="fa-solid fa-user-clock"></i>
                        </Button>
                    </span>
                    <span title="Choferes">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Reportes/Reportes_Choferes"); })">
                            <i class="fa-solid fa-id-card"></i>
                        </Button>
                    </span>
                    <span title="Historial Móviles">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Reportes/HistorialMoviles"); })">
                            <i class="fa-solid fa-truck"></i>
                        </Button>
                    </span>
                </div>
            </Card>
        </AntDesign.Col>

        @* Dependencias Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <Card Class="dashboard-card departamentos-card">
                <H2>UTILIDADES</H2>
                <div class="department-buttons">
                    <span title="Comunicación">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Dependencias/Comunicacion"); })">
                            <i class="fa-solid fa-walkie-talkie"></i>
                        </Button>
                    </span>
                    <span title="Depósito">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Dependencias/Deposito"); })">
                            <i class="fa-solid fa-warehouse"></i>
                        </Button>
                    </span>
                    <span title="Dependencias">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Dependencias"); })">
                            <i class="fa-solid fa-building"></i>
                        </Button>
                    </span>
                    <span title="Fuerzas Intervinientes">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/FuerzasIntervinientes"); })">
                            <i class="fa-solid fa-shield-halved"></i>
                        </Button>
                    </span>
                    <span title="Brigadas">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Brigadas"); })">
                            <i class="fa-solid fa-users-gear"></i>
                        </Button>
                    </span>
                    <span title="Equipos Autonomos">
                        <Button Type="ButtonType.Link" @onclick="@(() => { navigationManager.NavigateTo("/fire-force/Dependencias/EquiposAutonomos"); })">
                            <i class="fa-solid fa-person-booth"></i>
                        </Button>
                    </span>
                </div>
            </Card>
        </AntDesign.Col>

        @* Cobradores Section *@
        <AntDesign.Col Xs="24" Sm="12" Lg="8">
            <div @onclick="@(() => { navigationManager.NavigateTo("/fire-force/personal/cobradores"); })" style="cursor: pointer;">
                <Card Class="dashboard-card cobradores-card">
                    <i class="fa-solid fa-money-bill-wave card-icon"></i>
                    <h2>COBRADORES</h2>
                    <p>Ver el personal de cobradores</p>
                </Card>
            </div>
        </AntDesign.Col>
    </Row>
</div>

@code {
    private bool isOpen;
    private bool showIncendios;
    private bool showServicios;
    private bool showRescates;

    private void ToggleDropdown() => isOpen = !isOpen;

    private void toggleMenu(string menu)
    {
        switch (menu)
        {
            case "incendios":
                showIncendios = !showIncendios;
                showServicios = showRescates = false;
                break;
            case "servicios":
                showServicios = !showServicios;
                showIncendios = showRescates = false;
                break;
            case "rescates":
                showRescates = !showRescates;
                showIncendios = showServicios = false;
                break;
        }
    }
}